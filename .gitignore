# 백엔드(GAS) 코드는 의도적으로 저장소에 올리지 않는다.
# 이 저장소는 GitHub Pages로 공개 배포되므로 프론트(index.html, v2/)만 추적한다.
code.gs
*.gs

# 데이터 백업 스냅샷 (학생 개인정보 포함 — 절대 공개 저장소에 올리지 말 것)
backup/

# 나이스 오픈API 인증키 (개인 발급 — 공개 금지)
.neis_key

# 계정·비밀번호 (공개 저장소이므로 절대 추적하지 말 것)
CREDENTIALS.md
